spring:
  application:
    name: CustomPCBuilder
  banner:
    location: logo-app
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    defer-datasource-initialization: true
  datasource:
    url: jdbc:h2:file:./data/data
    username: sa
    password: